import itertools
import operator
import sys
from typing import Any, List, Optional, Tuple, Type, Union

from poker_game import error, poker, render


class Dealer:
    def __init__(
            self,
            ) -> None:
        pass
